import 'package:flutter/material.dart';

class SkillsScreenSmallSizeBodyFrameworkStackTitle2 extends StatelessWidget {
  const SkillsScreenSmallSizeBodyFrameworkStackTitle2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Stack",
        style: TextStyle(
          fontSize: 40,
          color: Colors.white,
        ),
      ),
    );
  }
}
